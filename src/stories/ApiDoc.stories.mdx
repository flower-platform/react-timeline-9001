import { Meta } from '@storybook/addon-docs';

<!-- "Canvas" tab is not needed, show only "Docs" tab -->
<Meta
    title="API doc"
    parameters={{
        viewMode: 'docs',
        previewTabs: { 
            canvas: {
                hidden: true
            }
        },
    }}
/>

<!-- Include the generated esdoc in an iframe.
The output directory for esdoc is docs, that is set as static directory in main.js. -->
<iframe
    src="./index-docs.html"
    width="100%"
    height="100%"
    title="apidocs"
    id="apidocs"
    frameBorder="0"
></iframe>