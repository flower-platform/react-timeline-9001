export const contextMenuScenarios = {
    whenRightClickThenContextMenu: 'WHEN right click, THEN a context menu is shown containing actions (each having a label and an icon)',
    sameActionDifferentLabel: 'Same action different label depending on a IGanttActionParameter',
    whenClickActionThenRunAndCloseMenu: 'WHEN click an action, THEN a specific action is run and the context menu is closed'
};